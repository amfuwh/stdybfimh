apiVersion: v1
kind: Service
metadata:
  name: xydjygzh
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "xydjygzh"
    port: 8080
  selector:
    app: xydjygzh
