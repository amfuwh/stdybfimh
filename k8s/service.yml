apiVersion: v1
kind: Service
metadata:
  name: gchdjyg
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "gchdjyg"
    port: 8080
  selector:
    app: gchdjyg
