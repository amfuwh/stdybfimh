apiVersion: v1
kind: Service
metadata:
  name: xhjsansx
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "xhjsansx"
    port: 8080
  selector:
    app: xhjsansx
