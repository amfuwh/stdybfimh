apiVersion: v1
kind: Service
metadata:
  name: jhbfmzs
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "jhbfmzs"
    port: 8080
  selector:
    app: jhbfmzs
