apiVersion: v1
kind: Service
metadata:
  name: ghcdjdzh
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "ghcdjdzh"
    port: 8080
  selector:
    app: ghcdjdzh
